## unittest模块中的断言方法
* assertEqual(a,b) 
  * 核实*a==b*
* assertNotEqual(a,b)
  * a!=b
* assertTrue(x)
  * x==**True**
* assertFalse(x)
  * x==**False**
* assertIn(item,list)
  * item in list
* assetNotIn(item,list)
* 